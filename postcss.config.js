import tailwindcss from 'tailwindcss';
import autoprefixer from 'autoprefixer';

export default {
    // On utilise un format de TABLEAU (Array) et non un objet
    plugins: [
        tailwindcss,
        autoprefixer,
    ],
};